"""Placeholder reporter handler.

Task 7 replaces this with the real budget-radar reporter logic
(discovering running resources, publishing the report to SNS, and
optionally invoking the budget action). This stub exists only so
`lambda.Code.fromAsset('lambda/')` has something to package while the
stack (Task 4) is developed and tested.
"""


def handler(event, context):
    return {}
